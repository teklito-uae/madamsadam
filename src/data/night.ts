import { pexelsUrl } from '@/lib/pexels'

/** Nightlife photography — first entry doubles as the section's hero background. */
export const nightImages: string[] = [
  pexelsUrl(5143166),
  pexelsUrl(33588956),
  pexelsUrl(29008748),
  pexelsUrl(29850671),
  pexelsUrl(18660106),
]
